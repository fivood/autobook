node_modules
build
.svelte-kit
package
.env
*.local

# Tauri
src-tauri/target
src-tauri/gen

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/*
!.vscode/settings.json
.idea

build-*.log
latest.json
release-notes-*.md
test-dicts/

# pdfjs sidecar assets (cmaps / standard_fonts / wasm), populated by
# scripts/copy-vendor-assets.js on npm install. ~200 upstream files —
# tracking them would churn the repo on every pdfjs bump.
static/vendor/pdfjs/
